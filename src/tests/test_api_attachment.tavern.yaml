test_name: Check user API

includes:
  - !include ../init/common.yaml

stages:
  - name: Get token for user
    request:
      url: "{tavern.env_vars.PROJECT_API_HOST}/auth/token/"
      verify: false
      method: POST
      json:
        username: "{test_username:s}"
        password: "{test_password:s}"
    response:
      status_code: 200
      save:
        body:
          auth_token: token

  - name: Get course groups
    request:
      url: "{tavern.env_vars.PROJECT_API_HOST}/planner/coursegroups/"
      verify: false
      method: GET
      headers:
        Authorization: "Token {auth_token:s}"
    response:
      status_code: 200
      save:
        body:
          course_group_id: 0.id

  - name: Get courses
    request:
      url: "{tavern.env_vars.PROJECT_API_HOST}/planner/coursegroups/{course_group_id}/courses/"
      verify: false
      method: GET
      headers:
        Authorization: "Token {auth_token:s}"
    response:
      status_code: 200
      save:
        body:
          course_id: 0.id

  - name: Create attachment
    request:
      url: "{tavern.env_vars.PROJECT_API_HOST}/info/"
      method: GET
      verify: false
    response:
      save:
        $ext:
          function: utils.attachmenthelper:create_attachment
          extra_kwargs:
            PROJECT_API_HOST: "{tavern.env_vars.PROJECT_API_HOST}"
            token: "{auth_token:s}"
            course_id: "{course_id:d}"

  - name: Verify attachment was uploaded
    request:
      url: "{attachment_url:s}"
      method: GET
      verify: false
    response:
      status_code: 200
